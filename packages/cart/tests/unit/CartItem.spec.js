import { mount } from '@vue/test-utils';
// import CartItem from '../../src/components/CartItem.vue';

describe('CartItem', () => {
    test('should 1+1 be 2', () => { 
        expect(1+1).toBe(2);
    })
})