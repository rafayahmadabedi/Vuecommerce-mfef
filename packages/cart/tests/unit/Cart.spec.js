import { mount } from '@vue/test-utils';
import Cart from '../../src/components/Cart.vue';

describe('CartItem', () => {
    xtest('should 1+1 be 2', () => { 
        expect(1+1).toBe(2);
    }),
    test('should calculate', () => { 
        expect(1 + 1).toBe(2);
    })
})